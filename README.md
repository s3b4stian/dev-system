# Dev System

Scripts to install dev environment.  

```
git clone https://github.com/s3b4stian/dev-system.git
```

Environment config
```
chmod +x ./dev-system/*.sh
./dev-system/start.sh
```

> **Note:** Every script can be run standalone without `sudo` command. Where sudo is required, the script stop and ask for the `sudo` password